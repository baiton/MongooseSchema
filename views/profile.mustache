<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="/profile_style.css">
  </head>
  <body>
    {{#cartoon}}
    <header>
      <form class="deleteB" action="/delete/{{_id}}" method="POST">
        <button type="submit" name="button">Delete</button>
      </form>
      <form class="" action="/character/edit/{{_id}}" method="POST">
        <button type="submit" name="button">Edit</button>
      </form>
    </header>
    <main>
      <h1>{{name}}</h1>
      <h2>{{show}}</h2>
      <img src="{{avatar}}" alt="{{name}}">
      {{/cartoon}}
    </main>

  </body>
</html>
